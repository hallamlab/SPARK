def homeDir = System.getProperty('user.home')
workDir = System.getenv('NXF_WORK') ?: "${homeDir}/.nextflow/work"
def condaCacheDir = System.getenv('NXF_CONDA_CACHEDIR') ?: "${workDir}/conda"

conda {
    enabled  = true
    useMamba = true
    cacheDir = condaCacheDir
    createTimeout = '2h'
}
